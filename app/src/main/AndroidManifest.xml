<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.terin.asu_flashcardapp">

    <!-- Allows access to other API levels for backwards compatibility
    <uses-sdk android:minSdkVersion="15"
        android:targetSdkVersion="21"
        android:maxSdkVersion="25" />
    -->


    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppMaterialTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddCourse"></activity>
        <activity android:name=".AddDeck"></activity>
        <activity android:name=".Options"></activity>
        <activity android:name=".CourseList"></activity>
        <activity android:name=".UserStats"></activity>
        <activity android:name=".CreateItems"></activity>
        <activity android:name=".CreateOptions"></activity>
        <activity android:name=".DeckList"></activity>
        <activity android:name=".StudyWindow"></activity>
    </application>

</manifest>